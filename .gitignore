# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Node
node_modules/
dist/
.vite/

# Secrets
.env
.env.*
*.key
credentials.json

# Data — raw API pulls are never committed
research/data/*.csv
research/data/*.xlsx
research/data/*.json
*.db
*.sqlite3
*.db-shm
*.db-wal
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# OS/editor
.DS_Store
.idea/
.vscode/

# Local project state — kept out of the repo intentionally
claude.md

# Raw per-request partials from ceda_pull.py. Local scratch that makes an
# interrupted backfill resumable; the assembled data/ceda_*.json files are
# what actually carry the data, so there is nothing to share here.
data/_ceda_cache/
